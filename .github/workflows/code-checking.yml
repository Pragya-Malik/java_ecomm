name: env   

on:
  push:
    branches:
      - main
  
env:
  COMPANY: Acme
  VERSION: 1.0

jobs:
  use-env:
    runs-on: ubuntu-latest
    steps:
      - name: print company name
        run: echo $COMPANY

      - name: print version
        run: echo $VERSION
        



  
